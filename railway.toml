[build]
builder = "NIXPACKS"
buildCommand = "npm --prefix bot install --omit=dev"

[deploy]
startCommand = "node bot/server.mjs"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
